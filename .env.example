FLASK_ENV=development
SECRET_KEY=gere-uma-chave-forte-aqui

# PostgreSQL — local (fora do Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cursos_certificados

# PostgreSQL — dentro do Docker Compose (use este no .env quando rodar com docker compose)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/cursos_certificados
